version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: uber-postgres
    ports:
      - "${POSTGRES_PORT:-5433}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    environment:
      - POSTGRES_USER=${POSTGRES_USER:-postgres}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD:-postgres}
      - POSTGRES_DB=${POSTGRES_DB:-uber}
      - POSTGRES_HOST_AUTH_METHOD=md5
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: uber-backend
    ports:
      - "${BACKEND_PORT:-3001}:3000"
    volumes:
      - ./backend:/app
      - /app/node_modules
    environment:
      - NODE_ENV=${NODE_ENV:-development}
      - DATABASE_HOST=postgres
      - DATABASE_PORT=5432
      - DATABASE_USER=${POSTGRES_USER:-postgres}
      - DATABASE_PASSWORD=${POSTGRES_PASSWORD:-postgres}
      - DATABASE_NAME=${POSTGRES_DB:-uber}
      - PRIVATE_KEY=${PRIVATE_KEY:-5J3GRKMPIVyxTysVCn3tPKeznaVnsGmr}
      - MAILGUN_API_KEY=${MAILGUN_API_KEY:-a4940c2a8c4fd28bb9abbc15a8784aa7-df55650e-57745318}
      - MAILGUN_DOMAIN=${MAILGUN_DOMAIN:-sandbox93a2774e692543ef984ec9714ce49028.mailgun.org}
      - MAILGUN_FROM_EMAIL=${MAILGUN_FROM_EMAIL:-ukiiing@askstory.com}

    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped

volumes:
  postgres_data:

